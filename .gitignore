# La base de données ne doit JAMAIS être envoyée sur GitHub :
# elle contient tes positions, et les données vivent maintenant sur Neon.
brvm_data.db
brvm_data.db-shm
brvm_data.db-wal

# Les secrets (mot de passe, adresse de connexion) se configurent
# dans l'interface de Streamlit, jamais dans le code.
.streamlit/secrets.toml

# Fichiers techniques de Python
__pycache__/
*.pyc
.venv/
venv/
